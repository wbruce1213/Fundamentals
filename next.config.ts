import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Turbopack is default in Next.js 16 — no extra config needed for this project.
  // For Vercel deployment: set ANTHROPIC_API_KEY or AI_GATEWAY_API_KEY in project settings.
};

export default nextConfig;
